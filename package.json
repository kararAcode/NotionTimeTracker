{
  "name": "notion-time-tracker",
  "version": "1.0.0",
  "main": "src/server/main.js",
  "scripts": {
    "start": "concurrently \"npm run server\" \"npm run client\"",
    "client": "vite",
    "server": "wait-on http://localhost:3000 && electron .",
    "dist": "electron-builder",
    "build": "vite build"
  },
  "devDependencies": {
    "concurrently": "^6.5.1",
    "electron": "^12.0.0",
    "wait-on": "^5.0.0"
  },
  "dependencies": {
    "autoprefixer": "^10.0.0",
    "electron-builder": "^24.13.3",
    "postcss": "^8.0.0",
    "react": "^17.0.0",
    "react-dom": "^17.0.0",
    "tailwindcss": "^2.0.0",
    "vite": "^5.3.2"
  }
}
